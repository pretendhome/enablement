# Calibration Exemplars — RIU-032: Extraction Pattern (Structured from Unstructured)

## Dimension 1: Extraction Accuracy

### Insufficient
"We have implemented a structured extraction system. The extracted fields are often incorrect and lack entity disambiguation and locale awareness."

This submission implements a structured extraction system but the extracted fields are often incorrect and lack entity disambiguation and locale awareness. It is superficial and does not provide a robust solution.

### Basic
"We have implemented a structured extraction system. The extracted fields are mostly correct but lack entity disambiguation and locale awareness."

This submission implements a structured extraction system with mostly correct fields but lacks entity disambiguation and locale awareness. It is not comprehensive.

### Competent
"We have implemented a structured extraction system. The extracted fields are correct with entity disambiguation and locale awareness."

This submission implements a structured extraction system with correct fields, entity disambiguation, and locale awareness. It provides a robust solution.

### Expert
"We have implemented a structured extraction system. The extracted fields are correct with entity disambiguation and locale awareness. Additionally, we have included mechanisms for updating the extraction accuracy based on feedback and performance data, ensuring continuous improvement."

This submission goes beyond competent by including mechanisms for updating the extraction accuracy based on feedback and performance data. It demonstrates a deep understanding of extraction accuracy and its continuous improvement.

## Dimension 2: Validation Coverage

### Insufficient
"We have implemented a structured extraction system. The system does not include validation to catch extraction errors before they propagate downstream."

This submission implements a structured extraction system but does not include validation to catch extraction errors. It is superficial and does not provide a robust solution.

### Basic
"We have implemented a structured extraction system. The system includes basic validation but does not catch all extraction errors before they propagate downstream."

This submission implements a structured extraction system with basic validation but does not catch all extraction errors. It is not comprehensive.

### Competent
"We have implemented a structured extraction system. The system includes validation that catches extraction errors before downstream propagation with ambiguity flagging."

This submission implements a structured extraction system with validation that catches extraction errors before downstream propagation and includes ambiguity flagging. It provides a robust solution.

### Expert
"We have implemented a structured extraction system. The system includes validation that catches extraction errors before downstream propagation with ambiguity flagging. Additionally, we have included mechanisms for updating the validation coverage based on feedback and performance data, ensuring continuous improvement."

This submission goes beyond competent by including mechanisms for updating the validation coverage based on feedback and performance data. It demonstrates a deep understanding of validation coverage and its continuous improvement.

## Dimension 3: Multi-Document Handling

### Insufficient
"We have implemented a structured extraction system. The system does not handle different document types with appropriate per-type extraction strategies."

This submission implements a structured extraction system but does not handle different document types with appropriate per-type extraction strategies. It is superficial and does not provide a robust solution.

### Basic
"We have implemented a structured extraction system. The system includes basic handling for different document types but lacks comprehensive per-type extraction strategies."

This submission implements a structured extraction system with basic handling for different document types but lacks comprehensive per-type extraction strategies. It is not comprehensive.

### Competent
"We have implemented a structured extraction system. The system handles different document types with appropriate per-type extraction strategies."

This submission implements a structured extraction system that handles different document types with appropriate per-type extraction strategies. It provides a robust solution.

### Expert
"We have implemented a structured extraction system. The system handles different document types with appropriate per-type extraction strategies. Additionally, we have included mechanisms for updating the multi-document handling based on feedback and performance data, ensuring continuous improvement."

This submission goes beyond competent by including mechanisms for updating the multi-document handling based on feedback and performance data. It demonstrates a deep understanding of multi-document handling and its continuous improvement.

## Dimension 4: Edge Case Robustness

### Insufficient
"We have implemented a structured extraction system. The system does not handle ambiguous, incomplete, and contradictory source text gracefully."

This submission implements a structured extraction system but does not handle ambiguous, incomplete, and contradictory source text gracefully. It is superficial and does not provide a robust solution.

### Basic
"We have implemented a structured extraction system. The system includes basic handling for ambiguous, incomplete, and contradictory source text but lacks comprehensiveness."

This submission implements a structured extraction system with basic handling for ambiguous, incomplete, and contradictory source text but lacks comprehensiveness. It is not robust.

### Competent
"We have implemented a structured extraction system. The system handles ambiguous, incomplete, and contradictory source text gracefully."

This submission implements a structured extraction system that handles ambiguous, incomplete, and contradictory source text gracefully. It provides a robust solution.

### Expert
"We have implemented a structured extraction system. The system handles ambiguous, incomplete, and contradictory source text gracefully. Additionally, we have included mechanisms for updating the edge case robustness based on feedback and performance data, ensuring continuous improvement."

This submission goes beyond competent by including mechanisms for updating the edge case robustness based on feedback and performance data. It demonstrates a deep understanding of edge case robustness and its continuous improvement.