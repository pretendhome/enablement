# Calibration Exemplars — RIU-022: Prompt Interface Contract

## Dimension 1: Contract Precision

### Insufficient
"We have defined a prompt interface contract with a few variables. The variables do not have explicit types, constraints, or validation rules."

This submission fails to define explicit types, constraints, or validation rules for the prompt variables. It is superficial and does not provide a robust contract.

### Basic
"We have defined a prompt interface contract with explicit variables. The variables have basic types but lack detailed constraints and validation rules."

This submission defines explicit variables with basic types but lacks detailed constraints and validation rules. It is not comprehensive.

### Competent
"We have defined a prompt interface contract with explicit variables, types, constraints, and validation rules. Each variable has a clear definition and validation criteria."

This submission defines explicit variables with types, constraints, and validation rules. It provides a comprehensive and robust contract.

### Expert
"We have defined a prompt interface contract with explicit variables, types, constraints, and validation rules. Each variable has a clear definition and validation criteria. Additionally, we have included mechanisms for updating the contract based on feedback and performance data, ensuring continuous improvement."

This submission goes beyond competent by including mechanisms for updating the contract based on feedback and performance data. It demonstrates a deep understanding of contract precision and its continuous improvement.

## Dimension 2: Output Schema

### Insufficient
"We have defined an output schema for the prompt interface. The schema is basic and does not include failure handling for non-conforming responses."

This submission defines a basic output schema but does not include failure handling for non-conforming responses. It is superficial and does not provide a robust solution.

### Basic
"We have defined an output schema for the prompt interface. The schema includes basic failure handling but is not comprehensive."

This submission defines an output schema with basic failure handling but lacks comprehensiveness. It is not robust.

### Competent
"We have defined an output schema for the prompt interface. The schema is enforced with comprehensive failure handling for non-conforming responses."

This submission defines an output schema that is enforced with comprehensive failure handling. It provides a robust solution.

### Expert
"We have defined an output schema for the prompt interface. The schema is enforced with comprehensive failure handling for non-conforming responses. Additionally, we have included mechanisms for updating the schema based on feedback and performance data, ensuring continuous improvement."

This submission goes beyond competent by including mechanisms for updating the schema based on feedback and performance data. It demonstrates a deep understanding of output schema and its continuous improvement.

## Dimension 3: Versioning

### Insufficient
"We have implemented basic versioning for the prompt interface. The versioning does not include breaking change detection or consumer compatibility."

This submission implements basic versioning but does not include breaking change detection or consumer compatibility. It is superficial and does not provide a robust solution.

### Basic
"We have implemented versioning for the prompt interface. The versioning includes basic breaking change detection but lacks consumer compatibility."

This submission implements versioning with basic breaking change detection but lacks consumer compatibility. It is not comprehensive.

### Competent
"We have implemented versioning for the prompt interface. The versioning includes breaking change detection and consumer compatibility."

This submission implements versioning with breaking change detection and consumer compatibility. It provides a robust solution.

### Expert
"We have implemented versioning for the prompt interface. The versioning includes breaking change detection and consumer compatibility. Additionally, we have included mechanisms for updating the versioning strategy based on feedback and performance data, ensuring continuous improvement."

This submission goes beyond competent by including mechanisms for updating the versioning strategy based on feedback and performance data. It demonstrates a deep understanding of versioning and its continuous improvement.

## Dimension 4: Composability

### Insufficient
"We have designed the prompt interface with basic composability. Shared prompt components are not versioned independently, and there is no cascade impact analysis."

This submission designs the prompt interface with basic composability but does not version shared components independently or include cascade impact analysis. It is superficial and does not provide a robust solution.

### Basic
"We have designed the prompt interface with composability. Shared prompt components are versioned independently, but there is no cascade impact analysis."

This submission designs the prompt interface with composability and versions shared components independently but lacks cascade impact analysis. It is not comprehensive.

### Competent
"We have designed the prompt interface with composability. Shared prompt components are versioned independently, and cascade impact analysis is included."

This submission designs the prompt interface with composability, versions shared components independently, and includes cascade impact analysis. It provides a robust solution.

### Expert
"We have designed the prompt interface with composability. Shared prompt components are versioned independently, and cascade impact analysis is included. Additionally, we have included mechanisms for updating the composability strategy based on feedback and performance data, ensuring continuous improvement."

This submission goes beyond competent by including mechanisms for updating the composability strategy based on feedback and performance data. It demonstrates a deep understanding of composability and its continuous improvement.